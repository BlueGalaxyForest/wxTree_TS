<view class="main">
  <block wx:for="{{treeList}}" wx:key="index">
    <tree treeItem="{{item}}" options="{{options}}" flatExpand="{{flatExpand}}"  keepAlive="{{keepAlive}}" step="1" bind:nodeClick="nodeClick"></tree>
  </block>
</view>