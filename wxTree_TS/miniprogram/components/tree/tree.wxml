<view class="main">
  <block wx:for="{{listData}}" wx:key="index">
    <tree listData="{{item}}" options="{{options}}" flatExpand="{{flatExpand}}"  keepAlive="{{keepAlive}}" step="1" bind:nodeClick="nodeClick"></tree>
  </block>
</view>